# model-peeklife-predictive-vm-management
PeakLife is a lightweight model that, given the historic utilization information for a VM, it predicts:  - Future CPU utilization: AvgCPU and MaxCPU (normalized) - Remaining lifetime: normalized remaining lifetime (and seconds via scaling)
